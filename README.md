# SwiftyWizard

`SwiftyWizard` is a Swift Package Manager framework.

For now, it contains one SwiftUI view:

```swift
SwiftyWizardView()
```

The demo app displays that view.

## Run The Demo

```sh
swift run SwiftyWizardDemo
```

## Build

```sh
swift build
```
